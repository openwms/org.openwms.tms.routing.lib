spring:
  jpa:
    database: postgresql
    generate-ddl: true
  datasource:
    driver-class-name: org.postgresql.Driver
    username: OPENWMS
    password: OPENWMS
    url: jdbc:postgresql:OPENWMSDB

owms:
  common-service:
    protocol: http
    name: common-service